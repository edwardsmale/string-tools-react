/* eslint-disable */
import React from 'react';
import { storiesOf } from '@storybook/react';
import Scrollbar from './Scrollbar';

storiesOf('Scrollbar', module).add('default', () => <Scrollbar />);
