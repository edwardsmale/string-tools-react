/* eslint-disable */
import React from 'react';
import { storiesOf } from '@storybook/react';
import ScrollViewer from './ScrollViewer';

storiesOf('ScrollViewer', module).add('default', () => <ScrollViewer />);
