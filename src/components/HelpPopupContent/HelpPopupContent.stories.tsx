/* eslint-disable */
import React from 'react';
import { storiesOf } from '@storybook/react';
import HelpPopupContent from './HelpPopupContent';

storiesOf('HelpPopupContent', module).add('default', () => <HelpPopupContent />);
