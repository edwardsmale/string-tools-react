/* eslint-disable */
import React from 'react';
import { storiesOf } from '@storybook/react';
import ContextPopupContent from './ContextPopupContent';

storiesOf('ContextPopupContent', module).add('default', () => <ContextPopupContent />);
